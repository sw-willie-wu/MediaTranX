# Changelog

本檔記錄 MediaTranX 每個正式版本的使用者可見變更。格式參考 [Keep a Changelog](https://keepachangelog.com/zh-TW/)。

## [Unreleased]

### 新增
- **流程畫布復原／重做**：Ctrl+Z 復原、Ctrl+Y（或 Ctrl+Shift+Z）重做，標題列的復原鈕同步可用；加刪節點、連線、移動、參數修改都可回退（每個分頁獨立記錄）
- **流程節點複製貼上**：選取節點後 Ctrl+C 複製（含彼此間的連線）、Ctrl+V 貼上，可跨分頁貼；連續貼上自動錯開位置
- **連線快捷鍵**：框選兩個節點按 C 直接連線（方向自動判定）；或按住 Shift 依序點兩個節點的連接圓圈完成連線

### 改進
- **有一條能跑就能執行**：畫布上只要有一條完整的流程即可執行，還沒接好的分支不再擋住執行按鈕（該分支不會被執行）
- **未連通節點一眼可辨**：沒接到輸入的節點與其連線以虛線淡化顯示，尚未接上的鏈頭保留紅框提醒；提示列在僅剩未連通分支的問題時轉為黃色（不擋執行）
- **不合理的連線直接不給連**：會成環、匯入同一節點、媒體類別不符的連線在拖曳時就無法放開建立，不再是連上後才跳警告

### 修正
- 框選節點後點擊其他節點，偶爾憑空出現連到輸出圓圈的連線的問題（單擊連接圓圈不再進入隱形的「待連線」狀態）

## [1.7.0] - 2026-07-14

### 新增
- **流程（節點模式）正式登場**：全新的節點畫布，把多個工具串成一條流程一次執行（例如影片 → 剪輯 → 轉檔 → 壓縮）；支援分岔並行、多檔批次輸入，中間產物預設不進產出抽屜（可逐節點勾選保留）；AI 助手也能用自然語言直接建立與執行流程
- **流程畫布分頁**：畫布頂部分頁列可同時開啟多份流程（上限 8）並獨立編輯；雙擊分頁改名、執行中的分頁顯示轉圈標記且不可關閉；執行中可切到其他分頁繼續編輯
- **流程檔匯出／匯入（.mtxflow）**：標題列的儲存鈕可把目前流程匯出成檔案（不含輸入檔）；把 .mtxflow 拖進流程頁畫布或用側欄「匯入流程檔」即可還原流程；拖到其他工具頁會引導前往流程頁開啟
- **常用流程**：側欄一鍵「新增常用」把目前流程存進清單；開啟常用流程一律開新分頁（已開啟則直接切換）

### 改進
- 任務名稱顯示統一：字幕相關任務在任務清單與歷史紀錄的名稱歸一，不再出現同功能多種叫法

### 修正
- 音量調整在特定操作順序下被誤設為 0 的問題
- 同頁多個模型選擇器互相覆蓋選擇的問題
- PDF 轉換輸出純文字時崩潰的問題
- 文件翻譯的語言選項在切換時被清空的問題

## [1.6.1] - 2026-07-11

### 新增
- **執行中可停止**：四個工具頁（圖片／音訊／影片／文件）執行中，執行按鈕變成紅色「停止」，確認後取消該頁所有進行中任務；取消中會顯示狀態直到真正停止
- **影片轉 GIF / APNG**：影片轉檔的輸出格式新增 GIF 與 APNG（無聲動圖、無限循環）；可調幀率（預設 12）與解析度，GIF 自動採用高品質調色盤；產出可直接送圖片壓縮工具後製
- **資料夾整批加入**：把整個資料夾拖進任何工具、或點上傳區的「選擇資料夾」即可批次加入（自動過濾不支援的檔案、含子資料夾）
- **任務同時執行上限**：設定 → 系統可調整同時執行的任務數（1–8）

### 改進
- **多圖切換不再卡頓**：圖片工具在多張大圖／GIF 之間切換即時流暢，圖片資訊載入大幅加速
- **本地 AI 互不搶佔**：AI 助手聊天與 GPU 任務（超解析、語音辨識等）不再互相中斷——任務執行中提問會友善告知稍候，助手回覆中提交的任務會自動排隊
- 工具頁左右側欄寬度可拖曳調整（雙擊分隔線還原預設）
- 影片的「畫面強化 / 補幀 / 影片摘要」現在支援多選批次執行；不支援批次的功能（裁切等）會明確提示
- 任務佇列狀態更誠實：排隊中的任務顯示「等待中」而非「執行中」，且排隊中的任務可以確實取消

### 修正
- 取消排隊中的任務後仍會被執行的問題
- 字幕（.srt/.vtt）等文字產出無法從產出抽屜「加入工具」的問題

## [1.6.0] - 2026-07-06

### 新增
- **意見回報**：可在「設定 → 關於」或任務失敗時直接回報問題，附上可預覽的診斷快照（使用者名稱自動遮罩），也可匯出診斷 zip 自行檢查
- **程式更新**：App 內檢查新版本、可設定自動檢查頻率（含「手動」），並直接下載安裝更新；測試版與正式版通道自動分流
- **圖片壓縮工具**：支援 JPEG / PNG / GIF / WebP 壓縮，PNG 可選失真/無損模式，GIF 依來源調色盤智慧設定、保證不會越壓越大，並顯示節省比例

### 改進
- **AI 助手**：確認策略改為四種模式（標準／自動／詢問／自訂）；標題列新增策略徽章（點擊切換）；工具名稱在對話中正確本地化；錯誤訊息更友善
- 遠端模型清單更新後即時同步到選單，不再需要重啟
- 啟動畫面顯示即時載入狀態；畫面異常崩潰時自動恢復、不再卡住
- 軟體更新設定移到「一般」分頁；設定頁版面加寬

### 修正
- GIF 壓縮在已最佳化的輸入上越壓越大的問題
- 中日韓檔名的 GIF 壓縮失敗問題
- 意見回報內容過大時送出失敗（413）的問題
- AI 助手按下停止後、已在執行中的操作仍會被套用的問題

## [1.5.2] - 2026-06-13

見 [GitHub Release](https://github.com/sw-willie-wu/MediaTranX/releases/tag/v1.5.2)。
