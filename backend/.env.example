# MediaTranX Backend 環境設定
# 複製此檔為 .env 並依需求修改（.env 已被 gitignore）
# Electron 啟動時會透過 env var 覆蓋（MEDIATRANX_PATH__* 格式）

# 資料根目錄（預設：core/backend/）
# MEDIATRANX_PATH__DATA=.

# Python venv 路徑（預設：{data}/.venv）
# MEDIATRANX_PATH__VENV=.venv

# 二進位工具目錄（預設：{data}/bin）
# MEDIATRANX_PATH__BIN=bin

# 模型目錄（預設：{data}/models）
# MEDIATRANX_PATH__MODELS=models

# 暫存目錄（預設：{data}/data/temp）
# MEDIATRANX_PATH__TEMP=data/temp
